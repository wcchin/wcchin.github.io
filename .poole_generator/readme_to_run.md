to build, run

```sh
poole --build --output ../
```

to build the website and put it at the `wcchin.github.io` directory



to see results

```sh
poole --serve --output ../
```

open a webpage at `localhost:8080`

